package vehicle;

import vehicle.Car;
import vehicle.Component;

public class Hybrid extends Car {
    public Hybrid(String model, String brand, int price, String color, Component component) {
        super(model, brand, price, color, component);
    }
}
